<!DOCTYPE html>
<html lang="en">

<head>
    <title>Broadboard</title>
    <meta name="description" content="BroadBoard - All the boards in the world" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="robots" content="index, follow" />
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <link rel="icon" href="/icon/house_icon-icons.com_70104.ico" />
    <link href="https://fonts.googleapis.com/css2?family=Pattaya&family=Poppins:wght@400;500&display=swap"
    rel="stylesheet">
</head>

<body>
    <header id="main-head">
        <nav>
            <h2 id="logo"><a href="/">BroadBoard</a></h2>
            <ul>
                <% if (user && user._id) { %>
                <li>
                    <a href="#">About</a>
                </li>
                <li>
                    <a href="/boards">Board</a>
                </li>
                <li>
                    <a href="/auth/logout">Logout</a>
                </li>
                <% } else { %>
                <li>
                    <a href="#">About</a>
                </li>
                <li>
                    <a href="/boards">Board</a>
                </li>
                <li>
                    <a href="/login">Login</a>
                </li>
                <li>
                    <a href="/join">Sign Up</a>
                </li>
                <% } %>
            </ul>
        </nav>
    </header>

    <% if (error && error.length > 0) { %>
    <div class="flash-error" role="alert">
        <h4><%= error %></h4>
    </div>
    <% } %>
    <% if (success && success.length > 0) { %>
    <div class="flash-success" role="alert">
        <h4><%= success %></h4>
    </div>
    <% } %>